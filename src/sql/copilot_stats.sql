--
-- This file provides a dummy content for copilot_stats query to be created in Query Tool.
--
SELECT
    (SELECT 12 FROM dual) AS available_copilots_count,
    (SELECT 23 FROM dual) AS available_copilot_projects_count
FROM dual;
